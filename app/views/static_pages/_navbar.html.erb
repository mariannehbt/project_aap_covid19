<!-- /Header -->
<nav class="navbar navbar-expand-md navbar-light bg-light">
    <a href="/" class="navbar-brand">EVAPSY</a>
    <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#navbarCollapse">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse justify-content-end" id="navbarCollapse">
        <div class="navbar-nav">
            <% if user_signed_in? %>
            <div class="nav-item dropdown">
                <a href="#" class="btn_1 rounded nav-link dropdown-toggle add_margin_10_right" data-toggle="dropdown">Mon compte</a>
                <div class="dropdown-menu">
                    <%= link_to "Editer Profil", edit_user_registration_path, :class =>'user', class: "nav-item nav-link" %>
                    <hr>
                    <%= link_to "Mon Espace", user_path(current_user), :class =>'user', class: "nav-item nav-link" %>
                </div>
            </div>
        </div>
        <hr>

        <div class="navbar-nav">

                <%= link_to "Déconnexion", destroy_user_session_path, method: :delete, :class =>'user', class: "btn_1 rounded " %>
                    
            <% else %>
            
                <%= link_to "Connexion", new_user_session_path, :class =>'user', class: "btn_1 rounded" %>
            
            <% end %>
        </div>
    </div>
</nav>

	